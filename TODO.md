# TODO - Document preview + notes

- [ ] Update `portfolio-site/script.js` to render inline previews for PDFs/images inside each document card.
- [ ] Add a “Document Notes” textarea below each preview/link for notes.
- [ ] Persist notes per file using `localStorage` and reload notes on render.
- [ ] Update `portfolio-site/styles.css` to style the preview area and note textarea.
- [ ] (Manual) Ensure `portfolio-site/uploads/index.json` has entries for each folder with `{name, href}`.
- [x] Implement inline preview + notes UI.
- [x] Persist notes per document using localStorage.
- [ ] (Manual) Verify in browser: preview works + notes persist after refresh.

